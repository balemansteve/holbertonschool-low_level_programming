#include "main.h"
/**
 * main - this program prints its name.
 *
 * @argv: string array that contain the arguments.
 * @argc: number of arguments passed of program.
 * Return: always 0.
 */
int main(int argc, char *argv[])
{
(void)argc;
printf("%s\n", argv[0]);
return (0);
}
