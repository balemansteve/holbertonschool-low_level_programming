#include "main.h"

/**
 * main - print number of arguments.
 *
 * @argc: number of arguments passed of the program.
 *
 * @argv: array of strings that contain the arguments.
 *
 * Return: Always 0.
 */
int main(int argc, char *argv[])
{
(void)argv;
printf("%d\n", argc);
return (0);
}
